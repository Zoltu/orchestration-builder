{
	"name": "adaptive-orchestrator",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"setup": "bun install --frozen-lockfile",
		"typecheck": "bun --bun tsc --noEmit",
		"test": "bun test --randomize --concurrent source/",
		"bench:smoke": "cd benchmarks/hello_001 && bun test tests/"
	},
	"devDependencies": {
		"@types/bun": "1.3.14",
		"typescript": "6.0.3"
	}
}
